{
    "data": [
        {{ $postCount := len .Data.Pages }}
        {{ range $index, $e := .Data.Pages }}
        {{ if $index }}, {{ end }} {
            "id" : "{{sub $postCount $index }}",
            "title" : "{{ .Title }}",
            "link" : "{{ .Permalink }}",
            "snippet": "{{ $e.Params.snippet }}",
            "soundcloud_id": "{{ $e.Params.soundcloud_id }}"
        }
        {{ end }}
    ]
}